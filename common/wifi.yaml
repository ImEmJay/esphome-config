captive_portal:

wifi:
  ssid: ${wifi_ssid}
  password: ${wifi_password}
  domain: ${wifi_domain}
  power_save_mode: ${wifi_power_save_mode}
  ap:
    ssid: "${device_name} AP"
    password: ${wifi_password}
